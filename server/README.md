# CampusFlow API

Express + MongoDB REST API. Copy `.env.example` to `.env`, then run `npm install`, `npm run seed`, and `npm run dev`.

Key routes: `/api/auth`, `/api/events`, `/api/dashboard`, `/api/notifications`. All protected routes use `Authorization: Bearer <jwt>`.
