import { Link, useNavigate } from 'react-router-dom'
import { ArrowUpRight, LogOut, Menu, UserRound } from 'lucide-react'
import { useState } from 'react'
import { useAuth } from '../context/AuthContext.jsx'
import Logo from './Logo.jsx'
export default function Navbar() { const { user, logout } = useAuth(); const navigate = useNavigate(); const [open, setOpen] = useState(false); const dashboard = user ? user.role === 'ADMIN' ? '/admin' : user.role === 'ORGANIZER' ? '/organizer' : '/dashboard' : '/login'; return <header className="navbar"><Link to="/" className="logo-link"><Logo /></Link><nav className={open ? 'nav-links open' : 'nav-links'}><Link to="/events" onClick={() => setOpen(false)}>Discover events</Link><a href="/#how-it-works" onClick={() => setOpen(false)}>How it works</a>{user ? <><Link to={dashboard} className="nav-dashboard" onClick={() => setOpen(false)}>Dashboard <ArrowUpRight size={14} /></Link><button className="nav-user" onClick={() => { logout(); navigate('/') }}><UserRound size={15} /> {user.name.split(' ')[0]} <LogOut size={13} /></button></> : <><Link to="/login" onClick={() => setOpen(false)}>Log in</Link><Link to="/register" className="nav-cta" onClick={() => setOpen(false)}>Join CampusFlow <ArrowUpRight size={14} /></Link></>}</nav><button className="menu-button" onClick={() => setOpen(!open)} aria-label="Menu"><Menu /></button></header> }
