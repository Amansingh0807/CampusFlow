import { Bookmark, CalendarDays, MapPin, Users } from 'lucide-react'
import { Link } from 'react-router-dom'
export default function EventCard({ event, onBookmark }) { return <article className="event-card"><div className={`event-image ${event.category?.slug || 'default'}`}><span>{event.category?.name || 'Opportunity'}</span><button onClick={() => onBookmark?.(event._id)} aria-label="Bookmark event"><Bookmark size={17} /></button></div><div className="event-card-body"><div className="event-card-top"><span className="mono">{new Date(event.date).toLocaleDateString('en-US', { month: 'short', day: 'numeric' })}</span><span className="seats"><Users size={13} /> {event.capacity - event.registeredCount} seats</span></div><h3>{event.title}</h3><p>{event.description}</p><div className="event-meta"><span><CalendarDays size={14} /> {event.startTime}</span><span><MapPin size={14} /> {event.mode === 'online' ? 'Online' : event.venue || 'Campus'}</span></div><Link className="card-link" to={`/events/${event._id}`}>Explore event <span>↗</span></Link></div></article> }
